# Changelog

本包遵循语义化版本。

## [0.1.0] - 2026-09-15

- 建立包骨架：`com.hm.meshmerge`，包含 `HmMeshMerge`（Runtime）与 `HmMeshMergeEditor`（Editor）两个程序集定义。
- 加入设计文档 `Documentation~/Design.md`（需求、数据契约、生成/运行两侧设计、校验规则与验收场景）。
