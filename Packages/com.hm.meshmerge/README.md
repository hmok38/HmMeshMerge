# Hm Mesh Merge

把共享同一 Shader 的多个模型合并为一个 Mesh 和一个材质；同一个网格只存一份几何、同一个材质只占一条数值列和一层贴图，来源与网格、材质的对应关系写在来源映射表里，绘制时按来源索引选择其中一个来源。插件不负责实例批次组织，单独合并 Mesh 不保证减少 draw call。

## 使用

1. 打开 Tools/HmMeshMerge/网格合并，新建或选择保存在 Assets 内的配置资产。
2. 添加 Mesh 与 Material。顺序就是来源索引，位置保持各源网格的局部坐标，不做排列偏移。同一个网格或同一个材质都可以被多个来源引用：几何只保留一份，材质也只占一条数值列和一层贴图，各来源仍按自己的材质取值。
3. 点击“按源着色器列出属性”。首次把不同值排前并启用，相同值排后并停用；以后只追加新项，保留旧行号和选择。贴图 Tiling/Offset 以属性名加 _ST 加入候选。
4. 选择所有源均未占用的索引通道，默认 **UV3（TEXCOORD3，零基编号）**。
5. 执行合并。纹理宽高、实际格式（含 sRGB）、mip 或采样设置不同会报错并给出建议值，**由用户手动统一，插件不修改源贴图**；每张需要修改的来源贴图各输出一条日志（同一贴图只输出一次），单击该条目即可在 Project 中定位对应贴图，最后再输出一次汇总——按贴图列出各层参数与建议，引用同一组贴图的属性共用一条。
6. 使用输出 Mesh 和 Material，以材质属性或 MaterialPropertyBlock 的 _MeshMergeIndex 选择来源（该值是来源索引，不是网格索引）。生成 Shader 中的逐属性函数可复制到自有 Shader；自有 Shader 必须在 Properties 中声明 _HmMeshMergeSources（来源映射表，两个索引在同一个纹素的通道里，布局见「来源映射表布局」）与 _HmMeshMergeParams 两个 2D 属性，生成的材质会自动绑定它们。

配置与结果都记录在同一个 HmMeshMergeAsset。停用参数使用第一来源的普通材质值，旧 LUT 行空出；只有显式“重新整理参数表”改变已有行号。改源材质数值后需要再次合并。

## 生成文件与数据契约

命名规则统一为 `{配置名}_{角色}.{扩展名}`：`{配置名}` 是配置文件不带扩展名的名字，全部产物都生成在配置资产所在目录。

| 生成文件 | 类型 | 作用 |
|---|---|---|
| `{配置名}_Mesh.asset` | Mesh | 合并网格：去重后各源网格的局部顶点、法线、切线、顶点色、UV0–UV7，并额外写入网格索引通道 |
| `{配置名}_Material.mat` | Material | 输出材质：绑定生成的纹理数组、参数 LUT、来源映射表；普通属性取第一来源的材质值 |
| `{配置名}_Shader.shader` | Shader | 生成的 URP 展示与接入模板：Properties、纹理声明、逐属性 HmRead/HmSample 函数、索引判断与 Alpha 裁剪 |
| `{配置名}_ParamLut.asset` | Texture2D | 参数 LUT：RGBAFloat、线性、无压缩、无 mip；X 为材质索引，Y 为稳定参数行，存放各来源的数值参数。无启用数值时不生成 |
| `{配置名}_SourceMap.asset` | Texture2D | 来源映射表：宽为来源数、高 1 的 RGBAFloat 贴图，把来源索引映射到网格索引与材质索引，见「来源映射表布局」 |
| `{配置名}_Array_{属性名}.hmtexarray` | Texture2DArray | 每个启用且各来源引用不同的二维贴图属性一份数组，层号就是材质索引 |
| `{配置名}_Array_{属性名}_{属性行号}.hmtexarray` | Texture2DArray | 属性名去掉下划线后彼此重名时的区分变体，行号取 Shader 属性下标 |
| `{配置名}.asset` | HmMeshMergeAsset | 配置本身：来源列表、参数表、索引通道与输出引用 |

- 同一个网格被多个来源引用时几何只写一份，顶点上存的是网格索引；同一个材质被多个来源引用时数值只写一列、贴图只写一层。可见性判断先用来源映射表把激活来源换成网格索引再比较；数值与纹理数组层号用表里换出的材质索引读取（顶点侧换一次后插值给片元），因此同一网格的不同材质组合各自保留自己的数值和贴图，也不会为重复材质多存贴图。
- 不再将数值夹到 0–1；Float、Range、Vector、Color、Integer 按类型读取。整数超出 Float 精确表示能力时报错。Color 在 Linear 项目中转换为线性值写入 LUT，其他数值不转换。
- 所有来源引用同一张贴图时不生成数组，直接沿用原贴图；未启用的不同参数也明确使用第一来源的值。
- 纹理数组用源贴图的每层 mip 原始数据保存，需手动开启 **Read/Write**，不支持 Crunch。导入器记录产物依赖；源贴图导入变化后数组随之重导。Android 实际格式与体积仍需人工出包核验。
- 已有 Mesh、Material、Shader、参数 LUT 和来源映射表按路径原地更新并保留 GUID，场景与 Prefab 的引用不会断；纹理数组按路径替换导入设置。改名前的旧产物（`{配置名}_Params.asset`、`{配置名}_Sources.asset`、更早的 `_Params.png`）不自动删除或迁移，重新合并后材质会改绑新名字的文件，旧文件可手动删除。
- 非三角形、蒙皮、BlendShape 会明确报错；请先烘焙成静态网格。多个子网格可合并，但每个来源只有一个材质，所有子网格使用该材质。
- 缺失的 UV/法线/切线补零，缺失顶点色补白；已有数值保留，合并后的顶点存储格式可能改变。基于顶点编号而非 UV/颜色的动画需要使用者自行适配。

### 来源映射表布局

`{配置名}_SourceMap.asset` 宽为来源数、高 1，RGBAFloat、线性、无 mip、Point/Clamp，按整数坐标 Load 读取；X 就是来源索引。**两个映射装在同一个纹素的通道里，不分成两行**：

| 通道 | 含义 |
|---|---|
| R | 该来源使用的网格索引（按网格引用去重后的下标） |
| G | 该来源使用的材质索引（按材质引用去重后的下标，同时也是 LUT 的列号与数组层号） |
| B、A | 保留，当前恒为 0 |

所以 `Load(int3(来源索引, 0, 0))` 一次取值就同时得到两个索引：`.x` 是网格索引，`.y` 是材质索引。以网格 A、网格 B 与材质 P、材质 Q 交叉组合的 4 条来源为例：

| 来源索引 | R（网格索引） | G（材质索引） |
|---|---|---|
| 0 | 0（网格 A） | 0（材质 P） |
| 1 | 1（网格 B） | 1（材质 Q） |
| 2 | 0（网格 A） | 1（材质 Q） |
| 3 | 1（网格 B） | 0（材质 P） |

用通道而不是两行（4×2），是因为顶点着色器里网格索引和材质索引总是同时需要：一次 Load 取回 float4 就够，分成两行就要两次 Load，而顶点纹理获取在移动端是昂贵操作；RGBAFloat 本来就有 4 个通道，B/A 留空不增加成本。写自有 Shader 时用 `HmMeshMergeLoadSourceMesh` / `HmMeshMergeLoadSourceMaterial` 取值，不要手写通道下标。

## Shader 接入

生成模板自动识别 MainTexture / MainColor 标记或 _BaseMap / _MainTex、_BaseColor / _Color 作基础展示；有 _Cutoff 时示范 Alpha 裁剪，有 _AlphaClip 时受其控制。这些仍是普通参数表项，没有独立配置字段。

生成的 HmRead 和 HmSample 函数包含实际行号及贴图取法，参数是材质索引；模板要求输出 Shader 声明 `_HmMeshMergeSources`（来源映射表）和 `_HmMeshMergeParams`（参数 LUT）两个 2D 属性，否则合并报错。三个 Pass 统一处理来源选择和 Alpha 裁剪：顶点着色器先用来源映射表换出网格索引判断可见性、换出材质索引插值给片元取数值。模板不重现源 Shader 的光照、透明混合、法线解码或其他专有效果。

生成模板对 `HmMeshMerge.hlsl` 的 `#include` 按工具运行工程里包的实际安装路径写入（嵌入包是 `Packages/<目录名>`，git 或本地安装是 `Packages/<包名>`），不写死包名。示例 Shader 是静态文件，路径固定为包名 `com.hm.meshmerge`，改名后需同步修改。

合并数据不依赖宿主项目或实例化框架。**当前生成模板与示例针对 URP**；其他管线需移植数据读取和索引判断。默认实例属性路径不承诺 SRP Batcher 兼容。矩阵 m33 编码仅保留为自定义渲染路径接口，不能把编码矩阵当作标准 TRS 使用。

完整职责和失败边界见 [设计说明](Documentation~/Design.md)。

## 验证状态

完成源码静态检查；未执行 Unity 编译、导入、测试或运行。请手动重新合并，检查切换来源、参数值、阴影/深度和二次合并后的已有引用，并确认同一网格的几何只剩一份、同一材质只占一层贴图。顶点着色器会用 Load 读来源映射表，请在目标平台确认顶点纹理获取可用。

**代码未编译，由用户人工编译验证。**
